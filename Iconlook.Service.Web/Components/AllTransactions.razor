@using System.Reactive.Linq
@BlockchainModel.AllTransactions.ToString("N0")
@code {
    protected override void OnInitialized()
    {
        Observable.Interval(TimeSpan.FromSeconds(2)).Subscribe(x =>
        {
            InvokeAsync(() => StateHasChanged());
        });
    }
}
